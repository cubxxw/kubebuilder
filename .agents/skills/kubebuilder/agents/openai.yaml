interface:
  display_name: "Kubebuilder"
  short_description: "Repo-specific patterns and workflows for kubebuilder"
  default_prompt: "Use the kubebuilder repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true